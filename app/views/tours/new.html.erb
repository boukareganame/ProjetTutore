<%# app/views/tours/new.html.erb %>
<div class="container mt-5">
  <h1 class="text-primary mb-4">Nouveau Tour pour <%= @tontine.description %></h1>

  <%= render 'form', tontine: @tontine, tour: @tour %>

  <%= link_to 'Retour aux Tours', tontine_tours_path(@tontine), class: "btn btn-secondary mt-3" %>
</div>
